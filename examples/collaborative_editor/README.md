# Collaborative Editor

This example shows real-time collaborative editing with `ui.codemirror` and `.with_crdt()`:
all browser tabs opening the page share the same document and see each other's cursors.
